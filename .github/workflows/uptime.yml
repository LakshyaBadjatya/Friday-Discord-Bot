name: Uptime Monitor
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render service
        run: curl -sSf -o /dev/null "https://friday-discord-bot-9u7u.onrender.com" || exit 1
